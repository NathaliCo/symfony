{% extends 'base.html.twig' %}

{% block body %}
<div class="centradito">
    <h1>Editar producto</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn-guardar btn-primary btn" type="submit" value="Editar" />
    {{ form_end(edit_form) }}
</div>
{% endblock %}
